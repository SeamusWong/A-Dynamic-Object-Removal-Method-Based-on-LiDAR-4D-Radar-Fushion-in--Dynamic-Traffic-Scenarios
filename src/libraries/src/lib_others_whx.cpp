#include "lib_others_whx.h"
/* 
template <typename T>
int VectorFind(const vector<T> &vec, T find)
{
    VectorFind(vec, find, 0);
}

template <typename T>
int VectorFind(const vector<T> &vec, T find, int start)
{
    for (int count = start; count < vec.size(); count++)
    {
        if (vec.at(count) == find)
        {
            return count;
        }
    }
    return -1;
} 
 */